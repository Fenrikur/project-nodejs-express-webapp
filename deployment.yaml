apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-updated-deployment
  labels:
    app: express-updated
spec:
  replicas: 1
  selector:
    matchLabels:
      app: express-updated
  template:
    metadata:
      labels:
        app: express-updated
    spec:
      containers:
      - name: express
        image: localhost:5000/express:updated
        ports:
        - containerPort: 3000
          name: "web"
---
apiVersion: v1
kind: Service
metadata:
  name: express-updated-service
spec:
  selector:
    app: express-updated
  ports:
    - protocol: TCP
      port: 80
      targetPort: "web"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: express-updated-ingress
spec:
  rules:
  - host: express-updated.localhost
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: express-updated-service
            port:
              number: 80
